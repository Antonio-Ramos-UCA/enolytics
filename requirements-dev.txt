# ENOLYTICS — dependencias del proyecto
# Instalación:  python3 -m pip install -r requirements.txt

# --- Datos y utilidades ---
pandas>=2.0
requests>=2.31
beautifulsoup4>=4.12
lxml>=4.9              # parser rápido para BeautifulSoup
python-dateutil>=2.8
pytrends>=4.9          # interés de búsqueda de Google Trends (inteligencia de mercado)
langdetect>=1.0.9    # idioma de las reseñas (proxy de procedencia)

# --- Scraping robusto (reseñas dinámicas) ---
playwright>=1.40       # tras instalar: python3 -m playwright install chromium

# --- NLP / minería de textos ---
scikit-learn>=1.3      # LDA, TF-IDF, utilidades
transformers>=4.35     # modelos BERT (análisis de sentimiento)
torch>=2.0             # backend de transformers
nltk>=3.8              # tokenización, stopwords en español

# --- Modelos analíticos ---
statsmodels>=0.14      # regresión PRCA (importancia por impacto) + p-valores
# OJO: nada de esto entra en requirements.txt. El NLP y la PRCA se ejecutan EN LOCAL y
# dejan CSV en datos/procesado/; el dashboard SOLO lee esos CSV. Streamlit Cloud tiene
# ~1 GB de RAM y no aguantaría torch.

# --- Analítica (IPA / IPCA / DIPA / DIPCA) ---
numpy>=1.24
scipy>=1.11

# --- Visualización / dashboard ---
streamlit>=1.30
plotly>=5.18
pydeck>=0.8            # mapas en streamlit (st.map / pydeck)
